local gruvbox = require("src.theme.presets.gruvbox")
local roshnivim = require("src.theme.presets.roshnivim")
local default = require("src.theme.presets.default")
local nord = require("src.theme.presets.nord")

local M = {
	gruvbox = gruvbox,
	roshnivim = roshnivim,
	default = default,
	nord = nord,
}

return M
